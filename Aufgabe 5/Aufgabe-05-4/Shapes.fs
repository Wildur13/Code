module Shapes
open Mini
open ShapesTypes

// a)
let smiley(): Shape =
    failwith "TODO"

// b)
let rec contains (s: Shape) (x: Nat, y: Nat): bool =
    failwith "TODO"

// c)
let rec rightmost (s: Shape): Nat =
    failwith "TODO"

let rec topmost (s: Shape): Nat =
    failwith "TODO"
