module Mergesort

// a)
let rec splitIntoRuns<'a when 'a: comparison> (xs: 'a list): 'a list list =
    failwith "TODO"

// b)
let rec merge<'a when 'a: comparison> (xs: 'a list) (ys: 'a list): 'a list =
    failwith "TODO"

// c)
let rec mergePass<'a when 'a: comparison> (runs: 'a list list): 'a list list =
    failwith "TODO"

// d)
let rec mergePasses<'a when 'a: comparison> (runs: 'a list list): 'a list =
    failwith "TODO"

// e)
let mergesort<'a when 'a: comparison> (xs: 'a list): 'a list =
    failwith "TODO"
